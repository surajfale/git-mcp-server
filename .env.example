# Example environment variables for local development
# Copy this file to `.env` (outside version control) or use your MCP client to point to it.

# OpenAI API key (required when ENABLE_AI=true)
OPENAI_API_KEY=sk-REPLACE

# AI configuration
ENABLE_AI=true
AI_PROVIDER=openai
AI_MODEL=gpt-4o-mini
AI_TEMPERATURE=0.2
AI_MAX_TOKENS=400

# Workspace for cloning remote repos
WORKSPACE_DIR=/tmp/git-workspaces

# Enforce SSH-only remote URLs (true/false)
FORCE_SSH_ONLY=true
# Git Commit MCP Server Configuration
# Copy this file to .env and customize the values for your deployment

# ============================================================================
# CORE SETTINGS
# ============================================================================

# Default path to Git repository (default: ".")
# DEFAULT_REPO_PATH=.

# Maximum number of bullet points in commit messages (default: 5)
# MAX_BULLET_POINTS=5

# Maximum number of lines in commit summary (default: 2)
# MAX_SUMMARY_LINES=2

# Name of the changelog file to maintain (default: "CHANGELOG.md")
# CHANGELOG_FILE=CHANGELOG.md

# ============================================================================
# GIT AUTHENTICATION SETTINGS
# ============================================================================

# Path to SSH private key for Git operations
# Used for SSH-based Git authentication
# GIT_SSH_KEY_PATH=/path/to/ssh/key

# Username for HTTPS Git authentication
# Used with GIT_TOKEN for HTTPS-based Git operations
# GIT_USERNAME=your-git-username

# Token/password for HTTPS Git authentication
# Personal access token or password for Git operations
# GIT_TOKEN=your-git-token-or-password

# ============================================================================
# LOGGING SETTINGS
# ============================================================================

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# ============================================================================
# USAGE EXAMPLES
# ============================================================================

# Example 1: Local development with debug logging
# DEFAULT_REPO_PATH=.
# LOG_LEVEL=DEBUG

# Example 2: Production with Git SSH authentication
# DEFAULT_REPO_PATH=/path/to/repo
# GIT_SSH_KEY_PATH=/home/user/.ssh/id_rsa
# LOG_LEVEL=INFO

# Example 3: Production with Git HTTPS authentication
# DEFAULT_REPO_PATH=/path/to/repo
# GIT_USERNAME=your-username
# GIT_TOKEN=ghp_your_github_token
# LOG_LEVEL=WARNING
